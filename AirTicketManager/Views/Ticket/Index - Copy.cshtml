@* @model List<AirTicketEntittes.TicketViewModel>
@{
    Layout = "_Layout";
}
<div class="container">
    <h2 class="mt-3">All Tickets</h2>

    <!-- Table displaying all tickets -->
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Ticket ID</th>
                <th>Passenger Name</th>
                <th>Departure Date</th>
                <th>Arrival Date</th>
                <th>Ticket Price</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var ticket in Model)
            {
                <tr>
                    <td>@ticket.TicketID</td>
                    <td>@ticket.PassengerName</td>
                    <td>@ticket.DepartureDate.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@ticket.ArrivalDate.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@ticket.TicketPrice.ToString("C")</td>
                    <td class="ticket-actions">
                        <!-- Action buttons with Bootstrap styles -->
                        <a href="@Url.Action("Details", "Ticket", new { id = ticket.TicketID })" class="btn btn-info btn-sm">View</a>
                        <a href="@Url.Action("Edit", "Ticket", new { id = ticket.TicketID })" class="btn btn-warning btn-sm">Edit</a>
                        <a href="@Url.Action("Delete", "Ticket", new { id = ticket.TicketID })" class="btn btn-danger btn-sm">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <!-- Link to create a new ticket -->
    <div class="create-ticket">
        <a href="@Url.Action("Create", "Ticket")" class="btn btn-primary">Create New Ticket</a>
    </div>

</div>

<!-- Optionally include page-specific scripts (no need to include layout-wide scripts again) -->
@section Scripts {
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
}


 *@

 @model List<AirTicketEntittes.TicketViewModel>
@{
    Layout = "_Layout";
}
Bellow is the jquery data table
<div class="container">
    <h2 class="mt-3">All Tickets</h2>

    <!-- Table displaying all tickets -->
    <table id="ticketsTable" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Ticket ID</th>
                <th>Passenger Name</th>
                <th>Departure Date</th>
                <th>Arrival Date</th>
                <th>Ticket Price</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var ticket in Model)
            {
                <tr>
                    <td>@ticket.TicketID</td>
                    <td>@ticket.PassengerName</td>
                    <td>@ticket.DepartureDate.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@ticket.ArrivalDate.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@ticket.TicketPrice.ToString("C")</td>
                    <td class="ticket-actions">
                        <!-- Action buttons with Bootstrap styles -->
                        <a href="@Url.Action("Details", "Ticket", new { id = ticket.TicketID })" class="btn btn-info btn-sm">View</a>
                        <a href="@Url.Action("Edit", "Ticket", new { id = ticket.TicketID })" class="btn btn-warning btn-sm">Edit</a>
                        <a href="@Url.Action("Delete", "Ticket", new { id = ticket.TicketID })" class="btn btn-danger btn-sm">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <!-- Link to create a new ticket -->
    <div class="create-ticket">
        <a href="@Url.Action("Create", "Ticket")" class="btn btn-primary">Create New Ticket</a>
    </div>

</div>

<!-- Optionally include page-specific scripts (no need to include layout-wide scripts again) -->
@section Scripts {
    <!-- jQuery (necessary for DataTables) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- DataTables JS -->
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>

    <!-- DataTables Bootstrap 4 Styling (Optional, for styling the DataTable) -->
    <script src="https://cdn.datatables.net/1.11.5/js/dataTables.bootstrap4.min.js"></script>

    <!-- DataTables CSS for styling -->
    <link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.min.css" />
    <link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/dataTables.bootstrap4.min.css" />

    <script>
        $(document).ready(function () {
            // Initialize DataTable
            $('#ticketsTable').DataTable({
                "paging": true, // Enable pagination
                "searching": true, // Enable search box
                "ordering": true, // Enable column sorting
                "lengthChange": true, // Allow changing number of rows per page
                "pageLength": 10, // Default number of rows per page
                "responsive": true // Make table responsive on mobile
            });
        });
    </script>
}
